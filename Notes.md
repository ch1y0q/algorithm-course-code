## Notes

P1036：KMP模板
难题：
P1041：两个数组的findKth。
P1043：DP方程很难理解。

### 贪心
#### P1030
mle的版本：对每一个白点，将其和每一个黑点之间的距离存进一个vector，然后每次取两端点均未被选用的pair中最小的一个。O(n^2)，占用空间太大。
ac的版本：从左向右扫描，对每个点找到它右侧的第一个异色且未选用的点。不需要额外存储空间。

#### P1031
比较简单的贪心。计算 $ d_{i} = \sqrt{d^2 - y_i^2} $，然后对每个点按照$ x_i - d_i $ 从小到大排序。对每个点，判断上一个加入的点的坐标$ x $是否满足$ x \in [x_i - d_i, x_i + d_i] $，若满足，则可以继续用上一个基站，否则，在位置$ x_i+d_i $新增基站。

### 网络流
#### P038
最小费用最大流（MCMF）。正确的模板比较难找。先跑bfs检查是否还有从s到t的流量没满的路径，一边跑一边记录这条路径，然后计算这条路径可以通过的流量和费用总和，分别加到总流量、总费用。
建图时，用一个虚拟的节点s与所有男嘉宾相连，一个虚拟的节点t与所有女嘉宾相连，流量均为1，费用均为0。男女嘉宾之间按照所给数据建边。

### 参考文章
* https://blog.csdn.net/lwd906485829/article/details/111580167