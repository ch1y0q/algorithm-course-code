#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#define rep(a,b,c) for (int a=b;a<=c;a++)
#define per(a,b,c) for (int a=b;a>=c;a--)

typedef long long ll;
using namespace std;

int main() {
    int m;
    cin >> m;
    while (m--) {
        // start here
    }
    return 0;
}